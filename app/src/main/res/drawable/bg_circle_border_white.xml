<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Garis tepi (stroke) putih tebal -->
    <!-- Ini adalah lingkaran luar untuk tombol capture -->
    <stroke
        android:width="4dp"
        android:color="#FFFFFF" />

    <!-- Mengatur ukuran default, tapi ini akan di-override oleh layout_width/height di XML -->
    <size
        android:width="80dp"
        android:height="80dp" />
</shape>